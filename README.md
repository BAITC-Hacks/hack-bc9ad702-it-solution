# «Аким на 5 часов» — AI-симулятор управления городом

Интерактивный симулятор, в котором пользователь распределяет единый виртуальный бюджет между пятью городскими направлениями и получает итоговый **Astana Quality of Life Score** вместе с объяснением компромиссов, рисков и рекомендаций.

## Быстрый запуск прототипа

Для запуска на Windows дважды откройте `run.bat`: он создаст виртуальное окружение `.venv`, если его ещё нет, и запустит локальный сервер. Также можно запустить сервер вручную из корня проекта:

```powershell
python server.py
```

Затем перейдите на `http://localhost:8081`.

### AI-анализ

При первом запуске `run.bat` попросит вставить OpenAI API‑ключ в терминал, затем сам создаст `.env` и запустит сервер. Ключ используется только сервером и не передаётся в браузер. Никогда не добавляйте `.env` в Git и не отправляйте ключ в чат; если это произошло, сразу отзовите ключ и создайте новый. После подтверждения пяти решений сервер отправляет их, бюджет и рассчитанный Score в OpenAI Responses API и возвращает объяснение результата.

### Редактирование данных модели

Вся предметная часть симулятора вынесена в [data/model-data.json](data/model-data.json): стартовый бюджет, горизонт, веса критериев, профили районов, меры M1–M14, синергии и несовместимости. `app.js` не содержит исходных чисел — он только загружает JSON, валидирует сумму весов и выполняет расчёт.

Это позволяет аналитикам корректировать синтетические данные, добавлять район или инициативу и собирать альтернативные городские профили без изменения расчётного движка. После правки файла нужно перезапустить `run.bat` и обновить страницу. Для воспроизводимости сохраняйте версию JSON вместе с демонстрационным сценарием.

## Чек-лист задач и функций

### Обязательный MVP

- [x] Подготовить синтетический датасет условных районов города.
- [x] Задать одинаковый стартовый виртуальный бюджет для каждого сценария.
- [x] Создать точный каталог M1–M14: направление, стоимость, лаг, тип «район/город» и эффекты по T1–C2.
- [x] Реализовать выбор ровно пяти уникальных управленческих решений с обязательным выбором района для районных мер.
- [x] Отображать выбранные инициативы, их общую стоимость и остаток бюджета.
- [x] Запретить добавление решений при превышении бюджета, повторе, несовместимости или более чем двух мерах одного направления.
- [x] Рассчитать изменения городских и районных показателей после выбранных решений.
- [x] Рассчитать итоговый Astana Quality of Life Score.
- [x] Реализовать AI-анализ: сильные стороны сценария, риски, компромиссы и последствия.
- [x] Показать понятный итоговый экран со всеми результатами.
- [x] Добавить инструкцию запуска, описание архитектуры и демонстрационный сценарий в README.
- [x] Проверить, что другой набор решений меняет итоговый балл.

### Интерфейс и пользовательский путь

- [x] Стартовый экран с описанием цели, исходного бюджета и правил: 5 мер, до 2 мер в направлении, минимум 3 направления.
- [x] Экран/блок состояния районов с ключевыми показателями.
- [x] Пять разделов для принятия решений.
- [x] Карточки инициатив с ценой, эффектом и возможными ограничениями.
- [x] Индикатор бюджета, обновляющийся после каждого выбора.
- [x] Валидации: нужны ровно 5 уникальных мер; максимум 2 меры в направлении; район обязателен для районной меры; бюджет и несовместимости проверяются автоматически.
- [x] Итоговый дашборд: общий балл, оценки по направлениям, влияние на районы и AI-вывод.
- [x] Кнопка «Начать заново», возвращающая пользователя к одинаковым исходным условиям.

### AI и модель расчёта

- [x] Определить прозрачную формулу базового Astana Quality of Life Score.
- [x] Задать веса пяти направлений и правила влияния инициатив на показатели районов.
- [ ] Учесть убывающий эффект повторных инвестиций в одно направление.
- [x] Учесть штрафы за игнорирование критически слабых направлений или районов.
- [x] Передавать AI структурированный контекст: стартовые данные, решения, бюджет, изменения показателей и итоговый балл.
- [x] Сформировать шаблон AI-ответа: резюме, сильные стороны, риски, компромиссы, последствия и улучшения.
- [ ] Обеспечить запасной детерминированный текстовый анализ, если AI API недоступен.

### Качество, данные и демонстрация

- [x] Не использовать персональные, чувствительные или закрытые данные.
- [x] Зафиксировать стартовые данные и бюджет для честного сравнения команд.
- [ ] Покрыть расчёт бюджета и итогового балла автоматическими тестами.
- [ ] Проверить сценарий превышения бюджета.
- [ ] Проверить сценарий с пятью допустимыми решениями.
- [ ] Подготовить 2–3 демонстрационных сценария с разными результатами.
- [ ] Оформить презентационные скриншоты или короткое демо.

### Опциональные функции

- [ ] Сравнение нескольких команд или сохранённых сценариев.
- [ ] Карта/графики изменений показателей по районам.
- [ ] AI-рекомендации по перераспределению бюджета.
- [ ] Непредвиденное городское событие с необходимостью скорректировать план.
- [ ] Генерация краткой презентации выбранного сценария.

## Подробное описание того, что нужно сделать

### 1. Определить границы MVP

Цель MVP — за одну короткую сессию дать пользователю почувствовать компромиссы городского управления. Пользователь получает те же стартовые условия: бюджет 100 условных единиц, одинаковые данные пяти районов и каталог M1–M14. Затем он выбирает ровно пять уникальных мер. На районных мерах обязателен выбор района, в одном направлении допускается не более двух мер, поэтому сценарий охватывает минимум три направления.

После пятого решения система пересчитывает показатели, показывает итоговый балл и объясняет, почему этот сценарий получился сильным или рискованным.

**Критерий готовности:** основной путь проходит без ручных действий разработчика: старт → пять решений → контроль бюджета → результат с объяснением.

### 2. Подготовить синтетические данные

В текущей реализации использован точный синтетический набор из пяти районов: Есиль, Алматы, Сарыарка, Байконур и Нура. Это учебная модель: все показатели лежат в диапазоне 0–100, где 100 означает лучшее состояние.

Для каждого района хранятся:

| Поле | Значение |
| --- | --- |
| `district_id`, `name` | Идентификатор и название условного района |
| T1–C2 | Десять частных критериев МАИ: транспорт, экология, соцсфера, безопасность и сервисы |
| population_weight | Вес района при подсчёте общего результата |

Каталог содержит точные 14 мер M1–M14. Для каждой заданы направление, стоимость, лаг, тип «район/город» и полный эффект по критериям. Меры M1/M3, M4/M7 и M5/M13 проверяются на несовместимость согласно математической модели.

**Критерий готовности:** данные хранятся в `data/model-data.json`, загружаются приложением без внешних закрытых источников и могут корректироваться без правки расчётной логики.

### 3. Настроить бюджет и правила выбора

Всем пользователям должен выдаваться одинаковый бюджет — **100 условных единиц**. Значение хранится в конфигурации, а не задаётся пользователем. Это обеспечивает честное сравнение сценариев.

Логика выбора должна:

- разрешать выбрать ровно пять уникальных инициатив, но не более двух в одном направлении;
- показывать стоимость каждого выбора и остаток бюджета;
- блокировать карточку, если её выбор превысит бюджет;
- не давать открыть итоговый результат, пока не выбраны пять допустимых мер;
- позволять заменить решение, пересчитав бюджет;
- позволять сбросить сценарий до исходного состояния.

**Критерий готовности:** сумма решений никогда не превышает бюджет ни визуально, ни на уровне бизнес-логики.

### 4. Реализовать модель влияния решений

Инициативы должны менять не только общий счёт, но и конкретные показатели районов. Для каждой выбранной меры необходимо применить её эффект к затронутым районам, ограничив значения диапазоном от 0 до 100.

Например:

- транспортное решение уменьшает транспортную нагрузку, но может требовать значительной части бюджета;
- озеленение повышает доступность зелёных зон и может косвенно улучшать комфорт;
- социальная инфраструктура повышает доступность ключевых услуг;
- безопасность повышает соответствующий показатель, особенно в районах риска;
- городской сервис повышает качество обращений, уборки, освещения или цифровых услуг.

В текущей версии применяется метод анализа иерархий (МАИ / AHP). Каждая мера содержит эффект по частным критериям и лаг в кварталах. За горизонт в 8 кварталов применяется не полный эффект, а:

    ΔI(m, k) = Effect(m, k) × (8 − Lag(m)) / 8

Эффекты накладываются на показатели затронутых районов и ограничиваются диапазоном 0–100. Для связки M10 + M12 добавлен синергический бонус B1 +2 в районе меры M10. AI не вычисляет итоговый балл: он получает готовый результат математической модели и объясняет его.

### 5. Рассчитать Astana Quality of Life Score

Итоговый балл детерминированный и воспроизводимый. МАИ использует десять частных критериев с глобальными весами:

    T1 = 0.10, T2 = 0.10  — транспорт
    E1 = 0.09, E2 = 0.11  — экология
    S1 = 0.11, S2 = 0.11  — социальная инфраструктура
    B1 = 0.09, B2 = 0.09  — безопасность
    C1 = 0.10, C2 = 0.10  — городские сервисы

    D(district) = Σ w(k) × I(district, k)
    Score = 0.7 × D_avg + 0.3 × D_min − N_crit

Здесь D_avg — средневзвешенное по доле населения значение районных индексов, D_min — индекс самого слабого района, а N_crit — количество частных показателей ниже 40. Таким образом модель явно штрафует сценарии, которые оставляют критические проблемы без решения. В интерфейсе показываются Score до и после, изменение районных индексов и оценки по пяти направлениям.

**Критерий готовности:** если изменить хотя бы одно решение, пересчитываются показатели и итоговый Score.

### 6. Добавить AI-анализ

AI не заменяет расчётную модель, а объясняет её результат человеческим языком. В AI передаётся структурированный набор данных: исходные показатели, выбранные инициативы, расходы, остаток бюджета, изменения по районам, итоговый балл, сильные и слабые направления.

Ожидаемый ответ AI должен содержать:

- краткое резюме сценария;
- сильные стороны выбранных решений;
- риски и нерешённые проблемы;
- ключевые компромиссы: что улучшили и чем пожертвовали;
- вероятные последствия для районов;
- 2–3 практичные рекомендации для улучшения сценария.

Важно запретить AI придумывать несуществующие цифры: в промпте следует потребовать опираться только на переданные данные и явно отмечать допущения. Если доступ к модели недоступен, приложение показывает сформированный по правилам анализ, чтобы MVP оставался работоспособным.

### 7. Собрать интерфейс

Интерфейс должен вести пользователя по сценарию без обучения. Рекомендуемая структура:

1. **Стартовый экран** — правила, бюджет, кнопка запуска.
2. **Состояние города** — краткие карточки районов и проблемные зоны.
3. **Пять решений** — вкладки или последовательные шаги с инициативами.
4. **Панель бюджета** — всегда видна, показывает расходы и остаток.
5. **Итог** — общий Score, изменения по направлениям/районам, AI-анализ и кнопка перезапуска.

Для хакатона особенно важна наглядность: используйте цветовые индикаторы, простые столбчатые диаграммы или карту районов. При этом итоговые цифры должны оставаться читаемыми и объяснимыми.

### 8. Предложенная техническая архитектура

Минимальная архитектура может состоять из следующих модулей:

| Модуль | Ответственность |
| --- | --- |
| Frontend | Интерфейс выбора решений, отображение бюджета и результатов |
| Simulation engine | Проверка правил, применение эффектов инициатив и расчёт Score |
| Data layer | Версионируемый `data/model-data.json`: районы, меры, веса, правила и синергии |
| AI analysis service | Формирование контекста, вызов AI и запасной анализ |
| Tests | Проверка бюджета, формулы Score и сценариев |

Подходящий стек для быстрого прототипа: React/Vite или Next.js для интерфейса, TypeScript для модели, локальные JSON-файлы для данных и API-обработчик для AI. Если AI API недоступен, сервис должен возвращать детерминированный fallback-ответ.

### 9. Протестировать ключевые сценарии

До демо необходимо проверить минимум следующие случаи:

- все пользователи начинают с одинаковым бюджетом и одинаковыми данными;
- нельзя выбрать инициативу сверх остатка;
- можно заменить решение, и бюджет корректно пересчитывается;
- без пяти решений нельзя получить финальный результат;
- пять допустимых решений формируют Score и объяснение;
- разные наборы решений дают разные итоговые показатели;
- AI получает корректные числа и не влияет на базовый расчёт;
- при ошибке AI API пользователь всё равно получает результат.

### 10. Подготовить README и демо

После реализации README должен быть дополнен фактическими командами запуска, переменными окружения для AI, описанием структуры папок, источником/лицензией синтетических данных и скриншотами. Для защиты подготовьте один сбалансированный и один рискованный сценарий: это наглядно докажет, что решения действительно меняют модель.

## Связь с критериями оценки

| Критерий | Как закрывается в проекте |
| --- | --- |
| Соответствие задаче и работоспособность | Полный сценарий из пяти решений, единый бюджет, контроль лимита и итоговый Score |
| Техническая реализация | Разделение интерфейса, симулятора, данных и AI-анализа; детерминированная модель расчёта |
| README и воспроизводимость | Локальный синтетический датасет, конфигурация, тесты, инструкции запуска и демо-сценарии |
| Ценность и применимость | Наглядная тренировка бюджетных компромиссов и сравнение последствий городских решений |
| Потенциал и оригинальность | Data-driven МАИ-модель: один расчётный движок работает с разными городскими профилями JSON; районный эффект, баланс/риски и AI-объяснение создают основу для сравнения команд, событий и масштабирования |

## Definition of Done

Проект готов к сдаче, когда пользователь может локально запустить приложение, получить одинаковые стартовые условия, выбрать пять допустимых инициатив без превышения бюджета, увидеть изменённые показатели районов и итоговый Astana Quality of Life Score, а также прочитать понятный AI-анализ сильных сторон, рисков и вариантов улучшения.
